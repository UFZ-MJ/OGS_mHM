/**
 * \file Configure.h.in
 *
 * #defines which gets set through CMake
 */
 #ifndef CONFIGURE_H
 #define CONFIGURE_H

#define OGS_QT_VERSION ${QT_VERSION_MAJOR}${QT_VERSION_MINOR}
#define SOURCEPATH "${CMAKE_SOURCE_DIR}"

#cmakedefine OGS_VERSION "${OGS_VERSION}"
#cmakedefine OGS_DATE "${OGS_DATE}"

// for tests
#cmakedefine OGS_EXECUTABLE "${OGS_EXECUTABLE}"
#define PUT_TMP_DIR_IN "${PROJECT_BINARY_DIR}/tests/"

#cmakedefine LIS
#cmakedefine MKL
#cmakedefine BRNS
#cmakedefine GEM_REACT

#cmakedefine _POSIX_SOURCE
#cmakedefine NO_ERROR_CONTROL

#cmakedefine OGS_VRED_PLUGIN
#cmakedefine OGS_PYTHON
#cmakedefine MSVC
#cmakedefine OGS_USE_OPENSG

#cmakedefine OGS_USE_VRPN

#cmakedefine HAVE_64_BIT

#endif // CONFIGURE_H
